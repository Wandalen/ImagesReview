// const bmp = require( 'bmp-js' );
// const fs = require( 'fs' );

// const bmpBuffer = fs.readFileSync( `${__dirname}/../../data/images/Image.bmp` );
// const bmpData = bmp.decode( bmpBuffer );
// console.log( 'rgb(' + bmpData.data.toJSON().data.slice( 0, 3 ) + ')' )
// OUTPUT: rgb(0,0,0)
